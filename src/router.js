import React from "react";

// const ContructionList = React.lazy(()=> import('./modules/construction-list/contruction-list'));

const route = [
    // { path: '/construction/construction-list', exact: true, name: 'construction-list', component: ContructionList  },
]

export default route;